# Use trusted source IDs for artist identity

Artist names are display metadata and cannot establish identity because unrelated performers can share a name. The app identifies YouTube Music Artists by channel ID, keeps every credited Artist on collaboration tracks, and uses name-derived Unidentified Artists only when no trusted ID exists. Remote images and Favorite Artist catalogs require the trusted ID; the app does not search by name as a fallback.
