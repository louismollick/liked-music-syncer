# AGENTS.md

When reporting information to me, be extremely concise and sacrifice grammar for sake of concision.

After any code change: run relevant lint/typecheck/test/build checks to make sure they pass; if unable, report why not.

After every code change: update `docs/` if goals/features/user-facing behavior changed.
